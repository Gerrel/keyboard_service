name: keyboard_service_example
description: keyboard_service example.
version: 0.0.1
homepage: https://github.com/Gerrel/keyboard_service
publish_to: none
environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  keyboard_service:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.0.0

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true